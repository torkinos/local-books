{
  "name": "@local-books/core",
  "version": "0.1.0",
  "private": true,
  "description": "Pure-TypeScript domain core for Local Books. Zero UI/platform dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test --ext .ts"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  },
  "dependencies": {}
}
